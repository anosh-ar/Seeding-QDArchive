requests>=2.31.0
google-genai>=0.8.0
pypdf>=4.0.0
pdfplumber>=0.11.0
python-docx>=1.1.0
openpyxl>=3.1.0
pandas>=2.0.0
tabulate>=0.9.0
matplotlib>=3.7.0     # class-distribution report (vector PDF charts)
pydantic>=2.0.0
python-dotenv>=1.0.0
# Extra extractors (each lazily imported — install only those you need):
xlrd>=2.0.1           # legacy .xls files
olefile>=0.46         # legacy .doc files (best-effort text recovery)
py7zr>=0.21.0         # .7z archives
rarfile>=4.2          # .rar archives — also needs unrar.exe on PATH on Windows
